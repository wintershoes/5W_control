# 旧主程序底盘迁移计划

## 目标与范围

目标：为旧程序 `pick_and_place_vision_http_integrated_double_fixed.py` 提供一个**单独的 Kuavo/Jaten 底盘适配文件**。跨区域导航统一为一种方式：预先在路网配置抓取站点、放置站点，再以 `DispatchGoalNodeName` 导航。调用侧保留旧程序的流程语义：读取位姿 -> 站点导航 -> 等待完成 -> 相对前后/横移 -> 安全停止。

本计划只处理底盘。机械臂、夹爪、升降高度、相机、视觉、语音等部分：**留空，不在本次迁移中修改。**

新代码应在上位机 ROS 环境中运行：`~/5W_control`；Windows 只用于编辑和同步。

## 已验证的接口

| 能力 | 新机器人接口 | 现场结果 | 可作为正式实现基础 |
|---|---|---|---|
| 读取地图位姿 | ROS `/move_base/amcl_pose` | 已读到稳定 `x/y/theta`；theta 由四元数转换 | 是 |
| 读取当前导航源 | ROS `/twist_mux_new/out/nav_source_used` | 已读到 `nav_mpc_path_track` 等来源 | 是 |
| 切换手动/自动模式 | HTTP `POST /command?cmd=<ChangeMode>` | 已成功 | 是 |
| 前进、后退 | HTTP `RobotMotion(vx, vy, vw)`，手动模式 | 已成功 | 是 |
| 原地旋转 | HTTP `SetRotationTheta(theta)` + ROS `RotationStatus`，自动模式 | 原生实现和反馈链路已确认，待现场执行回归 | 是 |
| 站点导航 | HTTP `DispatchGoalNodeName(name=[站点名])`，自动模式 | 已成功 | 是 |
| 运动中静音 | 底盘本地音乐文件已替换为静音版本 | 已成功 | 与适配代码无关 |

已验证的 HTTP 通用入口：

```text
POST http://192.168.26.22:8888/command?cmd=<URL 编码后的 JSON>
```

## 旧程序功能对账

| 旧程序能力 | 旧接口/逻辑 | 新接口/实现方案 | 当前状态 | 差异与处理方式 |
|---|---|---|---|---|
| 获取位姿 | `robot_pose_speed()['pose']` | `/move_base/amcl_pose` | 已完成 | 字段语义保持为 `x/y/theta`；新接口需由四元数算 theta。 |
| 预设任务导航 | `robot_exec_pre_task(task_id)` | `DispatchGoalNodeName(name=[node])` | 站点下发已验证 | 旧任务 ID 统一替换为新路网站点名，例如 `PICK_STATION`、`PLACE_STATION`。 |
| 直接坐标导航 | `robot_go_to(x,y,theta)` | 不保留该分支 | 已确定不迁入 | 所有目标均先配置为路网站点，再按站点名导航；不猜测使用未验证的 ROS service。 |
| 等待导航完成 | 轮询位姿、判断启动/稳定/超时 | `/path_sequence_executor/out/path_track_state` | 已实现，待程序现场回归 | 已实测正常到站最终为 `reached_node_id == end_node_id`、`num_edge_remain == 0`、`current_edge_id == 0`；HTTP 接收成功不代表到站。 |
| 导航后额外前进 | `fine_move_forward_lateral(extra_forward, 0)` | `RobotMotion` + AMCL 闭环 | 待实现与验证 | 需要自动模式导航完成后切到手动模式，再闭环前进。 |
| 前进/后退 | `robot_step_control` | `RobotMotion(vx,0,0)` + AMCL 闭环 | 基础脉冲已验证 | 旧接口是距离命令，新接口是速度命令；适配层用短脉冲、读位姿、迭代补偿保持距离语义。 |
| 左右横移 | `robot_step_control(left/right)` | `RobotMotion(0,vy,0)` + AMCL 闭环 | 未现场验证 | HTTP 字段已确认，但未验证底盘是否接受横向速度、速度方向和实际位移。先单独小距离测试。 |
| 原地角度补偿 | `robot_twist(angular)` | `SetRotationTheta` + `RotationStatus` 原生闭环 | 已实现，待现场执行回归 | 相对角由适配层换算为绝对 theta；速度、PID 和完成容差由底盘 `rotation_dispatch` 管理。 |
| 全局坐标精准定位 | 先转角，再前后、再左右，最多迭代 | 不作为主流程导航步骤 | 已确定不迁入 | 旧程序使用 task ID 时本来就跳过该步骤；新方案同样由路网站点承担到位导航。相对前后微调仍单独保留。 |
| 立即停止 | 旧接口发零速度 | `RobotMotion(0,0,0)` 重复发送 | 已有实现 | 只能在手动模式有效；导航中停止/取消接口尚未确认。 |
| 状态/故障拦截 | 旧 AMR 状态与厂商接口 | 上位机可读 AMCL，但没有 `jaten_msgs` Python 包 | 部分缺失 | 正式运行前由操作者确认急停、驱动、故障；后续若需要代码级安全判断，需由底盘提供 HTTP 状态接口或在上位机安装消息包。 |

## 尚未测试的底盘行为

以下项目未通过现场测试前，不能宣称旧程序的底盘流程已完整迁移：

1. **横移 `vy`**：以 1-2 cm 的左移、右移测试确认方向、最小可控距离和 AMCL 误差。
2. **闭环距离精度**：测试 20-40 cm 前进、后退，验证适配后的最终误差是否能稳定达到旧程序目标的 1 cm；若 AMCL 噪声较大，需要放宽为经过实测确认的阈值。
3. **闭环角度精度**：测试小角度和较大角度旋转，验证最终 theta 误差。
4. **完整模式链路**：`AUTO 站点导航 -> 到站确认 -> MANUAL 微调 -> AUTO 下一站导航`，确认每次切换均成功且不会遗留速度命令。
5. **导航完成/失败判定**：正常到站反馈和程序级成功条件已经确认并接入；还需采集一次持续障碍和路径不可达状态。目前受阻不会被判为成功，而是在导航超时后返回失败。
6. **导航取消/急停后的收尾**：新接口尚未确认导航任务取消命令；不得把手动零速度当成自动导航的取消方法。

## 正式实现设计

底盘接口统一放在唯一文件 `chassis_adapter.py` 中。文件内部按类区分 ROS 只读、HTTP 通信和高层闭环编排，旧主程序不直接接触 ROS topic 或 HTTP JSON。

对外方法保持旧程序可理解的语义：

```python
get_current_pose() -> {"x": float, "y": float, "theta": float} | None
navigate_to_station(station_name, target_theta=None, target_angle=None, ... ) -> bool
move_relative(forward=0.0, lateral=0.0, tolerance=..., ...) -> bool
rotate_to_theta(target_theta, timeout=30, ...) -> bool
rotate_to_angle(target_angle, timeout=30, ...) -> bool
rotate_relative(angle, timeout=30, ...) -> bool
stop() -> None
```

内部职责：

| `chassis_adapter.py` 内部组成 | 职责 |
|---|---|
| `ChassisReadAdapter` | ROS 标准消息读取：AMCL 位姿、当前导航来源。 |
| `ChassisHttpClient` | HTTP 通信：`ChangeMode`、`RobotMotion`、`DispatchGoalNodeName`、`SetRotationTheta`、零速度停止。 |
| `ChassisAdapter` | 对外高层接口：模式编排、位姿闭环、导航完成判定、原生旋转完成判定、超时/重试、统一返回 `bool`。 |
| `test/` | 每个风险动作独立测试；全部从唯一的 `chassis_adapter.py` 导入。 |

## 实施顺序

1. **整理工程入口**：修正 `README.md` 中已过时的文件名与 `/RobotMotion` 描述；让 `test/` 下脚本能从仓库根目录稳定导入适配器。
2. **补齐模式封装**：在 HTTP 客户端增加 `ensure_auto_mode()`；正式移动前保证手动，正式导航前保证自动。
3. **实现闭环相对移动**：保留旧程序的坐标变换和迭代上限，底层由单次距离命令改为小速度脉冲。每次脉冲后读 `/move_base/amcl_pose`；`finally` 中始终发送零速度。
4. **先验证横移与精度**：通过独立测试脚本确认 `vy`、前后距离、角度误差，再确定默认速度、单脉冲时长、容差和最大迭代次数。
5. **实现站点导航等待器**：下发 `DispatchGoalNodeName` 后订阅 PathTrackState，检测新任务出现并持续打印节点/边进度，最终以目标节点到达且剩余边清零确认成功；它只接受站点名，不伪造旧 task ID，也不保留坐标导航分支。
6. **验证完整切换链**：做一次无抓取的站点 A -> 微调 -> 站点 B 流程，记录 AMCL 位姿、模式和最终误差。
7. **接入旧主流程**：只替换旧程序的底盘调用点；抓取、放置、视觉等原逻辑先保持原状或留空，不在同一次改动中混入新硬件接口。

## 迁移配置草案

正式接入时，底盘配置应显式写在新配置区，不再沿用旧 `TASK_ID`：

```python
CHASSIS_HOST = "192.168.26.22"
CHASSIS_PORT = 8888

PICK_STATION = "待填写：新路网抓取站点名"
PLACE_STATION = "待填写：新路网放置站点名"

PICK_TASK_EXTRA_FORWARD = 0.0
PLACE_TASK_EXTRA_FORWARD = 0.0
PICK_FORWARD_DISTANCE = 0.30
PICK_BACK_DISTANCE = 0.35
PLACE_FORWARD_DISTANCE = 0.28
PLACE_BACK_DISTANCE = 0.30
```

## 统一后的底盘顺序

标准抓放流程的底盘部分固定为：

```text
1. 切到 AUTO
2. DispatchGoalNodeName(PICK_STATION)
3. 确认已到抓取站点
4. 切到 MANUAL，按 PICK_FORWARD_DISTANCE 前进到工作位
5. 执行抓取（非底盘，留空）
6. 保持 MANUAL，按 PICK_BACK_DISTANCE 后退到安全位
7. 切到 AUTO
8. DispatchGoalNodeName(PLACE_STATION)
9. 确认已到放置站点
10. 切到 MANUAL，按 PLACE_FORWARD_DISTANCE 前进到工作位
11. 执行放置（非底盘，留空）
12. 保持 MANUAL，按 PLACE_BACK_DISTANCE 后退到安全位
```

旧程序的标准流程也是这个顺序：先调用 `PICK_TASK_ID` 导航，抓取完成并后退后，再调用 `PLACE_TASK_ID` 导航，最后放置并后退。它在使用任务 ID 时会跳过 `PICK_FINE_POSITION` 和 `PLACE_FINE_POSITION`，所以新方案不再配置或使用这两个全局坐标。

## 当前迁移结论

底盘的**通信通道和基础动作已可用**，但还不能直接把旧抓放主程序的底盘部分替换为当前测试代码：缺少闭环相对移动、导航完成判定、横移实测和完整模式交接。

完成“尚未测试的底盘行为”中的前五项后，才进入第 7 步，将旧程序的底盘调用正式改接到 `chassis_adapter.py`。这样除底盘接口替换外，旧流程的两次导航、前进、后退、微调和失败返回逻辑可以保持一致；全局坐标导航明确删除，只保留路网站点导航这一种方式。

## 非底盘部分

机械臂：

夹爪：

升降高度：

摄像头与视觉：
